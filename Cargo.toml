[package]
name = "smallbox"
version = "0.6.1"
authors = ["andylokandy"]
description = "`Small Box` optimization: store small item on the stack or fallback to heap for large item."
repository = "https://github.com/andylokandy/smallbox"
documentation = "https://docs.rs/smallbox/"
keywords = ["box", "alloc", "dst", "stack", "no_std"]
license = "MIT"

[features]
default = ["std"]
std = []
unsize = []
